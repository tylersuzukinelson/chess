$('document').ready(function(){
	var startPos
	var endPos
	
	// returns true if given move is valid for 
	// a pawn; false otherwise
	function validMove(startPos, endPos){


	};

	function move(startPos, endPos){

	};


	move(x, y);
});