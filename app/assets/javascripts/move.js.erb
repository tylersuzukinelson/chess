$(document).ready(function() {

  var origin, destination, validMove;
  //var countMoves = 0;

  $('.draggable').draggable({
      start: function(event, ui){
        origin = $(this).attr('id'); // obtain origin coordinates of game piece
        console.log("origin: " + origin);
      }
      
  });

  $('.droppable').droppable({
    accept: ".draggable",
    //activate:
    //hoverClass: "drop-hover",
    drop: function(event, ui){
      destination = $(this).attr('id'); // obtain the coordinates of destination square
      console.log("destination: " + destination);
      // change coordinates of current game piece
      //var piece_coords = ;
    }
  });

  $('.grid').on('click', function(e) {
    e.preventDefault();  



    // if(countMoves%2 == 0){
    //   // determine origin coordinates
    //   origin = $(this).text().split(' ').join('');
    // }
    // else{
    //   // determine destination coordinates
    //   //destination = $(this).html().split(' ').join('');
    //   destination = $(this).text().split(' ');
    //   validMove = $(this).hasClass('highlight')
    // }
    // countMoves++;

    // if(countMoves%2 == 0 && validMove){
    //   //move piece

    // }
    // else{
    //   // nullify moves
    //   //alert("Not a valid move");
    // }

  });

});