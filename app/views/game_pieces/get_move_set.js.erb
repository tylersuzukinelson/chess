$('.grid').removeClass('highlight'); // clear all valid moves

<% @possible_moves.each do |move| %>
		$('#<%= "#{move[0]}_#{move[1]}" %>').addClass('highlight'); // indicate squares that piece can be moved to

	//$('#<%= "#{move[0]}_#{move[1]}" %>').addClass('valid-move'); // indicate squares that piece can be moved to
	//$('#<%= "#{move[0]}_#{move[1]}" %>').addClass('droppable');  //adds droppable class to only the squares that piece can move to
<% end %>