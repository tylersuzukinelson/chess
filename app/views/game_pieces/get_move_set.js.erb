$('.grid').removeClass('highlight');

<% @possible_moves.each do |move| %>
	$('#<%= "#{move[0]}_#{move[1]}" %>').addClass('highlight');
	$('#<%= "#{move[0]}_#{move[1]}" %>').addClass('droppable');
<% end %>